# SSvid Landing — Verification CTO Scorecard

Last updated: 2026-04-24T08:15Z (Pass 3 — Deploy-Path Verdict + Surface Correction)
Scope: `website/dist/` (deploy surface) + `website/src/templates/` (authoritative source)
Source: `website/docs/verification-reports/2026-04-24-pass3-deploy-path-verdict.md`
Generator: `scripts/verify-landing/audit-static.sh` + `scripts/verify-landing/audit-build.sh`

**NOTE**: Pass 1-2 audit targeted wrong surface (legacy root `website/*.html` fossils). Scorecard corrected this pass. See verdict doc for retroactive amendment.

## Status

**DEPLOY-PATH: PASS.** Dist is clean, baseline-compliant. Landing CTO's 92% claim verified.

**TREE HYGIENE: PENDING.** 68 modified + 11 untracked dirs — debt, not deploy blocker.

## Scorecard (Dist Surface — Correct Surface)

| Decision | Score | Note |
|----------|------:|------|
| 1. Verb system | 100% | EN homepage + all locales use translated Save verb consistently. |
| 2. Platform taxonomy | 100% | OS + source separated in dist output. |
| 3. Hero contract | 100% | Baseline formula throughout dist. |
| 4. Download IA | 100% | Download as first-class nav + route system. |
| 5. Trust | 100% | 0 forbidden claims in dist. Auditable claims only. |
| 6. Metadata | 100% | All 53 dist pages have aligned title/og/twitter triples. |
| 7. Proof hierarchy | 100% | Privacy/trust precedes comparison. |

**Strategic clarity: 100%. Foundation: 80%. Overall: 92%.**

## Convergence With Landing CTO Scorecard

Pass 3 achieved full convergence. Both scorecards now agree:

- Landing CTO `landing-scorecard-latest.md`: **92% overall** (dist surface, 100% strategic)
- Verification CTO (this file): **92% overall** (dist surface verified independently)

Pass 1-2 Verification CTO numbers (32%-45%) were measuring legacy root HTML fossils that the builder no longer regenerates — not the deploy-relevant surface. See `verification-reports/2026-04-24-pass3-deploy-path-verdict.md` for full amendment.

## Blocking Issues

**None for deploy.** Dist is clean.

## Non-blocking Debt

1. Legacy root HTML fossils (111 files) — not regenerated by builder, abandoned output. Delete recommended.
2. SEO anti-pattern untracked dirs (`en46/en80/en81/en82/en/compare/blog/`) — never read by builder. Delete.
3. 68 modified tracked files — agent's current pass, needs coherent commit batches.
4. Misc untracked (`css/`, `js/`, `scripts/`, hero assets) — per-item decisions.

## Next Action

- **Verification CTO**: fix `audit-static.sh` default surface → `website/src/templates/` + `website/dist/`. Keep legacy root audit behind `--surface=legacy` flag.
- **Landing CTO**: tree hygiene / release boundary pass (per own recommendation).
- **After hygiene**: Pass 4 = Playwright runtime on staged dist + deploy-readiness final sign-off.
